extends layout

block main
  h2 Generate an AI image...

  form.forms(action='/generate' method='post')
    dl
      dt
        label(for='imagePrompt') What do you want to see?
      dd
        input.search(type='text'
              id='imagePrompt'
              name='imagePrompt'
              placeholder='e.g a kangaroo playing the guitar...')

    fieldset.actions
      input.buttons.generate(type='submit' value='Generate')

  if imageUrl
    img(src=imageUrl, alt='The picture generated by the ai image generator.')

  if imageUrl && albums.length > 0
    form.forms#secondary-form(action='/save_image' method='post')
      label(for='albumId') Save to album:
      fieldset.actions
        select.buttons.new(name='albumId' id='albumId')
          option(value='none' selected disabled hidden) Select Album:
          each album in albums
            option(value=album.id)=album.name
        input.buttons.new(type='submit' value='Save Image')
  if !albums.length && !imageUrl
    p(style='text-align: center;') Note: You cannot save an image unless you create an album.
    p(style='text-align: center;') To create one, 
      a(href="/new_album" style='color: #0a1c3b; font-weight: bold;') click here.
